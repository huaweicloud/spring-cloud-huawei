spring:
  application:
    name: price
  cloud:
    servicecomb:
      discovery:
        version: 0.0.1
      config:
        serverAddr:  http://127.0.0.1:30113
        watch:
          delay: 10000
      credentials:
        enabled: true
        accessKey: your ak
        secretKey: your sk
        akskCustomCipher: default
        project: cn-east-2