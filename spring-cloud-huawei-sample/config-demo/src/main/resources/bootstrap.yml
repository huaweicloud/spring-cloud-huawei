spring:
  application:
    name: price
  cloud:
    servicecomb:
      discovery:
        version: 0.0.1
      config:
        serverAddr: https://cse.cn-east-2.myhuaweicloud.com
        watch:
          delay: 10000
      credentials:
        enable: true
        accessKey: your ak
        secretKey: your sk
        akskCustomCipher: default
        project: cn-east-2