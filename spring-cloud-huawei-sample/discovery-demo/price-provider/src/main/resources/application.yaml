server:
  port: 8080

spring:
  application:
    name: price
  cloud:
    servicecomb:
      discovery:
        enabled: true
        address: http://127.0.0.1:30300,https://cse.cn-east-2.myhuaweicloud.com
        serviceName: price
        version: 0.0.1
        healthCheckInterval: 30
      credentials:
        enable: true
        accessKey: your ak
        secretKey: your sk
        akskCustomCipher: default
        project: cn-east-2
