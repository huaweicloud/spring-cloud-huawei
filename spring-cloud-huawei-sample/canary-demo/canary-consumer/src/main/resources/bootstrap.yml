spring:
  application:
    name: canary-consumer
  cloud:
    servicecomb:
      config:
        serverAddr: https://cse.cn-north-4.myhuaweicloud.com
        watch:
          delay: 10000
      credentials:
        enable: true
        accessKey: ak
        secretKey: sk
        project: cn-north-4
        akskCustomCipher: default